# Penumbra fixture — Markdown (tint: violet)

A sample with **bold**, *italic*, ~~strikethrough~~, `inline code`, and a [link](https://example.com).

## Lists & blockquotes

- bullet one
- bullet two with `code`
- bullet three

1. ordered first
2. ordered second
3. ordered third

> A blockquote demonstrating `inline code` inside a quoted paragraph.
> Second line of the same quote.

## Code fences

```ts
const x: number = 42;
console.log(`x = ${x}`);
```

```python
def hello(name: str) -> str:
    return f"Hello, {name}"
```

## Tables

| Column A | Column B | Column C |
|---------:|:--------:|:---------|
|        1 |   Ada    | admin    |
|        2 |  Grace   | guest    |

## Inline

Footnote ref[^1] and a horizontal rule below.

---

[^1]: Footnote text goes here.
